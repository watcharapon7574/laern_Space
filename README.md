# คลังเก็บสื่อการสอน (Media Teaching Library)

ระบบคลังเก็บสื่อการสอนออนไลน์ที่สร้างด้วย Next.js 14 สำหรับการจัดเก็บ จัดการ และเล่นสื่อการสอนจากลิงก์ URL (โดยเฉพาะจาก loveable.dev)

## คุณสมบัติหลัก

### 🎯 คุณสมบัติสำหรับผู้ใช้
- **หน้าแรก**: แสดงสื่อยอดนิยม หมวดหมู่ และแท็กยอดนิยม
- **เล่นสื่อ**: เล่นสื่อผ่าน iframe ในเว็บไซต์โดยตรง
- **ค้นหา**: ค้นหาสื่อตามชื่อ คำอธิบาย หรือแท็ก
- **หมวดหมู่**: เรียกดูสื่อตามหมวดหมู่ (เกม, วิทยาศาสตร์, คณิต, ภาษาไทย, ภาษาอังกฤษ, สังคม, อื่น ๆ)
- **รองรับ Dark Mode**: ใช้งานได้ทั้งในโหมดสว่างและมืด
- **Responsive Design**: ใช้งานได้ทั้งบนมือถือและคอมพิวเตอร์

### 🔐 คุณสมบัติสำหรับผู้ดูแล
- **แดชบอร์ด**: ภาพรวมสถิติการใช้งาน
- **จัดการสื่อ**: เพิ่ม แก้ไข และลบสื่อการสอน
- **ดึงข้อมูลอัตโนมัติ**: ดึง metadata (ชื่อ, คำอธิบาย, รูปภาพ) จาก URL
- **ตรวจสอบความปลอดภัย**: อนุญาตเฉพาะ URL จาก domain ที่กำหนด

### 📊 การติดตาม Analytics
- **นับ Views**: ติดตามการเข้าชมสื่อ
- **นับ Plays**: ติดตามการเล่นสื่อ
- **Rate Limiting**: ป้องกันการ spam จาก IP เดียวกัน
- **สถิติแบบเรียลไทม์**: แสดงสถิติการใช้งานแบบเรียลไทม์

### 🔒 ความปลอดภัย
- **Content Security Policy (CSP)**: ป้องกัน XSS attacks
- **Domain Allowlist**: อนุญาตเฉพาะ domain ที่ปลอดภัย
- **IP Hashing**: เก็บ IP ในรูป hash เพื่อความเป็นส่วนตัว
- **Secure Headers**: ใช้ security headers มาตรฐาน

## เทคโนโลยี

- **Framework**: Next.js 14 (App Router)
- **Database**: SQLite (Prisma ORM)
- **Authentication**: NextAuth.js
- **UI**: TailwindCSS + shadcn/ui
- **Language**: TypeScript

## การติดตั้งและรัน

### 1. ติดตั้ง dependencies
```bash
npm install
```

### 2. ตั้งค่า environment variables
ไฟล์ `.env` มีค่าเริ่มต้นดังนี้:

```env
DATABASE_URL="file:./dev.db"
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3001"
ALLOWLIST_DOMAINS="loveable.dev,*.loveable.dev"
```

### 3. Setup database
```bash
# สร้าง database และ tables
npx prisma db push

# เติมข้อมูลตัวอย่าง (ถ้ายังไม่มี)
npm run db:seed
```

### 4. รัน development server
```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่ [http://localhost:3001](http://localhost:3001)

## การใช้งาน

### สำหรับผู้ดูแลระบบ

1. **เข้าสู่ระบบ**: ไปที่ `/auth/signin`
   - อีเมล: `admin@example.com`
   - รหัสผ่าน: `admin123`

2. **เพิ่มสื่อการสอน**: 
   - ไปที่ `/admin/add`
   - ใส่ URL สื่อการสอน (จาก loveable.dev)
   - ระบบจะดึง metadata อัตโนมัติ
   - เลือกหมวดหมู่และเพิ่มแท็ก

3. **ดูสถิติ**: ไปที่ `/admin` เพื่อดูแดชบอร์ด

### สำหรับผู้ใช้ทั่วไป

1. **เรียกดูสื่อ**: ดูสื่อยอดนิยมบนหน้าแรก
2. **ค้นหา**: ใช้ช่องค้นหาหรือไปที่ `/search`
3. **เลือกหมวดหมู่**: คลิกหมวดหมู่ที่สนใจ
4. **เล่นสื่อ**: คลิกที่สื่อเพื่อเล่นผ่าน iframe

## โครงสร้างโปรเจกต์

```
src/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── auth/              # Authentication pages
│   ├── admin/             # Admin interface
│   ├── categories/        # Category pages
│   ├── media/             # Media detail pages
│   ├── search/            # Search page
│   └── layout.tsx         # Root layout
├── components/            # React components
├── lib/                  # Utility libraries
├── types/               # TypeScript type definitions
└── middleware.ts        # Security middleware
```

## API Endpoints

### Media Management
- `GET /api/media` - รายการสื่อ
- `POST /api/media` - เพิ่มสื่อใหม่ (admin only)
- `PATCH /api/media/[id]` - แก้ไขสื่อ (admin only)
- `DELETE /api/media/[id]` - ลบสื่อ (admin only)

### Analytics
- `POST /api/track/view` - บันทึกการดู/เล่น

### Metadata
- `POST /api/metadata` - ดึง metadata จาก URL (admin only)

## สถานะโปรเจกต์

✅ **เสร็จสิ้นแล้ว**:
- ระบบฐานข้อมูล (Prisma + SQLite)
- การยืนยันตัวตน (NextAuth.js)
- UI Components (shadcn/ui + TailwindCSS)
- ระบบจัดการสื่อ (CRUD APIs)
- ระบบติดตามการใช้งาน (Analytics)
- ความปลอดภัย (CSP, Security Headers)
- หน้าแรก, ค้นหา, หมวดหมู่
- แดชบอร์ดผู้ดูแล
- Responsive Design + Dark Mode

🚀 **พร้อมใช้งาน**: โปรเจกต์สามารถใช้งานได้เต็มรูปแบบแล้ว

## 🔧 การแก้ไขปัญหา

✅ **แก้ไขปัญหาแล้ว**:
- Middleware error - ย้ายไฟล์ middleware.ts ไปยัง root directory
- Image loading error - เพิ่มการกำหนดค่า next.config.js สำหรับ external images
- ปรับปรุง URL รูปภาพในข้อมูลตัวอย่างให้เสถียรขึ้น

## ข้อมูลตัวอย่าง

ระบบมีข้อมูลตัวอย่าง 10 รายการจาก loveable.dev ในหมวดหมู่ต่าง ๆ:
- เกมคณิตศาสตร์
- วิทยาศาสตร์
- ภาษาไทยและอังกฤษ  
- สังคมศึกษา
- เครื่องมือสร้างสรรค์

## License

MIT License